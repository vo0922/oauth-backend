server:
  port: 8082
  servlet:
    context-path: /api/v1

spring:
  application:
    name: resource-server

# JWT 관련 설정 (public key만 필요)
jwt:
  public-key-path: classpath:/keys/public.pem
  issuer: auth-server
